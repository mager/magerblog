---
import type { CollectionEntry } from 'astro:content';
import FormattedDate from './FormattedDate.astro';

interface Props {
	post: CollectionEntry<'blog'>;
}

const { post } = Astro.props;
---

<a href={`/blog/${post.id}/`} class={`card ${!post.data.heroImage ? 'no-image' : ''}`}>
	{post.data.heroImage && (
		<img src={post.data.heroImage} alt="" width={720} height={360} />
	)}
	<div class="content">
		<p class="date">
			<span class="prompt">$</span> <FormattedDate date={post.data.pubDate} />
		</p>
		<h4 class="title">{post.data.title}</h4>
		<p class="description">{post.data.description}</p>
	</div>
</a>

<style>
	.card {
		display: block;
		background: var(--editor-bg-light);
		border-radius: 12px;
		box-shadow: var(--box-shadow);
		overflow: hidden;
		transition: all 0.3s ease;
		text-decoration: none;
		color: inherit;
		border: 1px solid var(--text-muted);
		position: relative;
	}

	.card::before {
		content: '';
		position: absolute;
		top: 0;
		left: 0;
		right: 0;
		height: 3px;
		background: linear-gradient(90deg, var(--cyan) 0%, var(--purple) 50%, var(--accent-green) 100%);
		opacity: 0;
		transition: opacity 0.3s ease;
	}

	.card:hover {
		transform: translateY(-4px);
		box-shadow: var(--box-shadow-lg);
		text-decoration: none;
		border-color: var(--cyan);
	}

	.card:hover::before {
		opacity: 1;
	}

	.card img {
		width: 100%;
		height: auto;
		aspect-ratio: 16 / 9;
		object-fit: cover;
		border: none;
		border-bottom: 2px solid var(--text-muted);
		transition: border-color 0.3s ease;
	}

	.card:hover img {
		border-bottom-color: var(--purple);
	}

	.card.no-image {
		background: linear-gradient(135deg, var(--editor-bg-dark) 0%, var(--editor-bg-light) 100%);
	}

	.card.no-image .content {
		padding-top: 2rem;
	}

	.content {
		padding: 1.5rem;
	}

	.title {
		margin: 0 0 0.5rem;
		font-size: 1.3rem;
		color: var(--text-primary);
		font-weight: 800;
		letter-spacing: -0.02em;
		transition: color 0.3s ease;
	}

	.card:hover .title {
		color: var(--cyan);
	}

	.date {
		font-size: 0.875rem;
		color: var(--text-secondary);
		margin: 0 0 0.5rem;
		font-family: 'JetBrains Mono', monospace;
		letter-spacing: 0.5px;
		font-weight: 500;
	}

	.date .prompt {
		color: var(--accent-green);
		font-weight: 700;
		margin-right: 0.25rem;
	}

    .description {
        margin: 0;
        font-size: 1rem;
        color: var(--text-secondary);
        line-height: 1.7;
    }
</style>
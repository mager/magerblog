---
import type { CollectionEntry } from 'astro:content';

interface Props {
	post: CollectionEntry<'blog'>;
	index?: number;
}

const { post, index = 0 } = Astro.props;

const isDraft = !!post.data.draft;
const displayTitle = isDraft ? `[DRAFT] ${post.data.title}` : post.data.title;
const d = post.data.pubDate;
const dateStr = `${d.getFullYear()}-${String(d.getMonth() + 1).padStart(2, '0')}-${String(d.getDate()).padStart(2, '0')}`;
const monthNames = ['JAN', 'FEB', 'MAR', 'APR', 'MAY', 'JUN', 'JUL', 'AUG', 'SEP', 'OCT', 'NOV', 'DEC'];
const monthShort = monthNames[d.getMonth()];
const dayStr = String(d.getDate()).padStart(2, '0');
const yearStr = String(d.getFullYear());
const tags = (post.data.tags || []).slice(0, 3);
const idxStr = String(index + 1).padStart(2, '0');
const dek = tags.length > 0 ? tags.join(' • ') : 'ai • systems • shipping notes';
---

<a href={`/blog/${post.id}/`} class={`code-row ${isDraft ? 'draft' : ''}`}>
	<span class="row-index">{idxStr}</span>
	<span class="row-date">{dateStr}</span>
	<span class="row-body">
		<span class="row-title">{displayTitle}</span>
		<span class="row-dek">{dek}</span>
	</span>
	<span class="row-tags">
		{tags.map(tag => <span class="tag">{tag}</span>)}
		<span class="row-arrow" aria-hidden="true">↗</span>
	</span>

	<span class="mobile-card" aria-hidden="true">
		<span class="mc-topline">
			<span class="mc-idx">{idxStr}</span>
			<span class="mc-date">{monthShort} {dayStr}, {yearStr}</span>
		</span>
		<span class="mc-title">{displayTitle}</span>
		<span class="mc-dek">{dek}</span>
	</span>
</a>

<style>
	.code-row {
		display: grid;
		grid-template-columns: 3.25rem 8rem minmax(0, 1fr) auto;
		gap: 1rem;
		align-items: start;
		padding: 1rem 0.2rem;
		text-decoration: none;
		color: inherit;
		border-bottom: 1px solid rgba(230, 237, 243, 0.08);
		transition: transform 0.18s ease, background 0.18s ease, border-color 0.18s ease;
	}

	.code-row:hover {
		transform: translateX(4px);
		background: rgba(121, 192, 255, 0.04);
		border-color: rgba(121, 192, 255, 0.18);
	}

	.code-row.draft {
		opacity: 0.65;
	}

	.row-index,
	.row-date,
	.row-dek,
	.tag,
	.row-arrow,
	.mc-topline,
	.mc-dek {
		font-family: 'IBM Plex Mono', monospace;
	}

	.row-index {
		font-size: 0.78rem;
		letter-spacing: 0.14em;
		text-transform: uppercase;
		color: rgba(139, 148, 158, 0.7);
		padding-top: 0.25rem;
	}

	.row-date {
		font-size: 0.74rem;
		letter-spacing: 0.1em;
		text-transform: uppercase;
		color: #7ee787;
		padding-top: 0.25rem;
	}

	.row-body {
		display: grid;
		gap: 0.34rem;
		min-width: 0;
	}

	.row-title {
		font-family: 'IBM Plex Sans Condensed', sans-serif;
		font-size: clamp(1.18rem, 2vw, 1.65rem);
		line-height: 0.98;
		letter-spacing: -0.04em;
		text-transform: uppercase;
		color: #e6edf3;
	}

	.row-dek {
		font-size: 0.69rem;
		text-transform: uppercase;
		letter-spacing: 0.11em;
		color: rgba(139, 148, 158, 0.85);
		line-height: 1.5;
	}

	.row-tags {
		display: flex;
		align-items: center;
		gap: 0.45rem;
		flex-wrap: wrap;
		justify-content: flex-end;
		padding-top: 0.2rem;
	}

	.tag {
		font-size: 0.64rem;
		text-transform: uppercase;
		letter-spacing: 0.08em;
		padding: 0.34rem 0.48rem;
		border: 1px solid rgba(121, 192, 255, 0.16);
		background: rgba(121, 192, 255, 0.06);
		color: #79c0ff;
	}

	.row-arrow {
		font-size: 1rem;
		color: #79c0ff;
		opacity: 0;
		transform: translateX(-6px);
		transition: opacity 0.18s ease, transform 0.18s ease;
	}

	.code-row:hover .row-arrow {
		opacity: 1;
		transform: translateX(0);
	}

	.mobile-card {
		display: none;
	}

	@media (max-width: 720px) {
		.code-row {
			display: block;
			padding: 0;
			transform: none;
			background: transparent;
		}

		.row-index,
		.row-date,
		.row-body,
		.row-tags {
			display: none;
		}

		.mobile-card {
			display: grid;
			gap: 0.45rem;
			padding: 0.95rem 0;
		}

		.mc-topline {
			display: flex;
			align-items: center;
			gap: 0.65rem;
			font-size: 0.66rem;
			letter-spacing: 0.12em;
			text-transform: uppercase;
			color: rgba(139, 148, 158, 0.78);
		}

		.mc-idx {
			color: #7ee787;
			font-weight: 700;
		}

		.mc-title {
			font-family: 'IBM Plex Sans Condensed', sans-serif;
			font-size: 1.5rem;
			line-height: 0.98;
			letter-spacing: -0.04em;
			text-transform: uppercase;
			color: #e6edf3;
		}

		.mc-dek {
			font-size: 0.66rem;
			letter-spacing: 0.11em;
			text-transform: uppercase;
			line-height: 1.5;
			color: rgba(139, 148, 158, 0.84);
		}
	}
</style>
